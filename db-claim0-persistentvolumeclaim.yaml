apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    app: db-claim0
  name: db-claim0
  namespace: asterisk
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: db-pv0
  resources:
    requests:
      storage: 100Mi
